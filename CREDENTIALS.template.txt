========================================================================
 TB MedTrack — CREDENTIALS  (FILL THIS IN, THEN SAVE AS  CREDENTIALS.txt)
========================================================================

HOW TO USE
1. Copy this file and rename the copy to:  CREDENTIALS.txt
   (CREDENTIALS.txt is git-ignored, so it will NOT be committed.)
2. Fill in the values below.
3. When you deploy on Railway, copy each value into Railway's
   "Variables" tab (see backend/DEPLOY_RAILWAY.md).

DO NOT paste these anywhere public (chat, screenshots, GitHub).
If a token is ever exposed, rotate it in Turso immediately.

------------------------------------------------------------------------
 1) TURSO  (get these from the Turso dashboard or CLI)
------------------------------------------------------------------------
# Your database URL (looks like libsql://name-user.aws-region.turso.io)
TURSO_DATABASE_URL = libsql://medicine-harishbag211.aws-ap-south-1.turso.io

# Your NEW auth token (rotate the old leaked one first!):
#   turso db tokens invalidate medicine
#   turso db tokens create medicine
TURSO_AUTH_TOKEN = PASTE_YOUR_NEW_TOKEN_HERE

------------------------------------------------------------------------
 2) BACKEND
------------------------------------------------------------------------
# A long random string you invent (used to sign device sessions).
# Example: run  openssl rand -hex 32   or just type ~40 random characters.
SESSION_SIGNING_SECRET = PASTE_A_LONG_RANDOM_STRING_HERE

# Railway sets PORT automatically; leave blank unless running locally.
PORT =

------------------------------------------------------------------------
 3) FIREBASE CLOUD MESSAGING  (optional — only for push to father's phone)
------------------------------------------------------------------------
# Leave blank to skip push for now. To enable later, see docs/BACKEND.md.
GOOGLE_APPLICATION_CREDENTIALS =

------------------------------------------------------------------------
 4) AFTER DEPLOY — put your backend's public URL here
------------------------------------------------------------------------
# Railway gives you a URL like https://tbmedtrack-backend.up.railway.app
# You'll enter this in the app later to turn on sync.
API_BASE_URL = PASTE_YOUR_RAILWAY_URL_AFTER_DEPLOY
